<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_pinned_pages_sync"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.apps.anker.facepunchdroid.PinnedPagesSyncActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/ppsActionbar"
        android:layout_height="?attr/actionBarSize"
        android:layout_width="match_parent"
        android:minHeight="?attr/actionBarSize"
        app:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        android:background="?attr/colorPrimary"
        android:animateLayoutChanges="true"
        android:visibility="visible"
        />

    <TextView
        android:text="Pinned pages sync"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/textView"
        android:layout_marginStart="16dp"
        android:layout_marginTop="14dp"
        android:layout_below="@+id/ppsActionbar"
        android:layout_alignParentStart="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Get Folders"
        android:layout_below="@+id/textView"
        android:layout_alignStart="@+id/textView"
        android:layout_marginTop="17dp"
        android:id="@+id/btnGetFolders" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:text="Folder Name"
        android:ems="10"
        android:id="@+id/editTextFolderName"
        android:layout_below="@+id/btnGetFolders"
        android:layout_alignStart="@+id/btnGetFolders" />

    <Button
        android:text="Create folder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btnCreateFolder"
        android:layout_below="@+id/editTextFolderName"
        android:layout_alignStart="@+id/editTextFolderName" />

</RelativeLayout>
